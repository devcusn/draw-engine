export type PropertiesType = {
  scale: number;
  offsetX: number;
  offsetY: number;
  isDragging: boolean;
  lastX: number;
  lastY: number;
  gridSize: number;
  gridLineWidth: number;
  gridLineColor: string;
};
